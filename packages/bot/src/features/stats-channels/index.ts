/**
 * Statistics Channels — Phase 10
 *
 * Voice channels that display live server stats (members, online, roles, etc.).
 * Updates on a configurable interval, respects Discord rate limits.
 */
export { StatsChannelManager } from './stats-manager.js';
