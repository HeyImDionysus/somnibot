{
  "name": "@somnibot/launcher",
  "version": "1.0.0",
  "private": true,
  "description": "SomniBot Desktop Launcher \u2014 download, configure, run.",
  "main": "dist/main/bootstrap.js",
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist/main/release-source.json',{force:true})\"",
    "build": "tsc && tsc -p tsconfig.preload.json && node -e \"require('node:fs').cpSync('src/renderer','dist/renderer',{recursive:true})\"",
    "start": "electron dist/main/bootstrap.js",
    "dev": "tsc && electron dist/main/bootstrap.js",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "clean": "node ../../scripts/clean.mjs dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "electron-store": "^10.0.0",
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^39.8.5",
    "electron-builder": "^26.15.3",
    "electron-builder-squirrel-windows": "26.15.3",
    "typescript": "^5.7.0",
    "vite": "^7.3.5",
    "vitest": "^3.2.4"
  }
}
