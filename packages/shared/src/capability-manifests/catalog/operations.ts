import { buildFeatureManifest } from '../build-manifest.js';

const operation = buildFeatureManifest;

export const OPERATIONS_MANIFESTS = [
  operation({ id: 'role-channel-management', name: 'Roles, Permissions, and Channels', domain: 'administration', summary: 'Authoritative guild structure, hierarchy, permission, and dependency management.', users: ['owner', 'administrator'], surfaces: { dashboardRoutes: ['/roles', '/channels'], scenarioProofs: ['administration-rbac'] }, journey: 'An owner imports live structure, corrects hierarchy, previews blast radius, and applies a verified change.', invalidState: 'A role is above the bot, a channel reference is stale, permissions escalate, or paid access would be broken.', discordBehavior: 'Structure changes apply in dependency order and stop on permission or hierarchy mismatch.', dashboardBehavior: 'Live and intended roles, tiers, channels, permissions, references, and conflicts are compared explicitly.', syntheticEvidence: 'Generated hierarchies prove tier inference, manual overrides, cycles, deleted references, and least privilege.', liveEvidence: 'A reversible test role and channel change is applied and read back from Discord.', crossFeature: 'Every role- or channel-bearing feature declares references for conflict and blast-radius analysis.', services: ['configuration release engine', 'Discord structure reconciler'], tables: ['guild_config'] }),
  operation({ id: 'server-setup-sync', name: 'Server Setup and Sync', domain: 'administration', summary: 'Import, preview, deploy, and reconcile complete guild configuration.', users: ['owner', 'administrator'], surfaces: { dashboardRoutes: ['/server-setup', '/sync'], botFeatures: ['setup-wizard'], scenarioProofs: ['administration-server-sync'] }, journey: 'An owner imports source facts, selects tracks, previews target changes, deploys, and verifies live readback.', invalidState: 'Source and target guilds are confused, prerequisites fail, or a change would mutate a read-only source.', discordBehavior: 'Deployment mutates only the selected target and records every created or updated Discord identity.', dashboardBehavior: 'Diff, prerequisites, operation progress, failures, compensation, and readback remain visible.', syntheticEvidence: 'Source-target fixtures prove read-only source handling, dependency ordering, partial failure, resume, and rollback.', liveEvidence: 'A disposable target guild is configured through dashboard and bot while source remains unchanged.', crossFeature: 'The adoption map and all feature manifests contribute scoped setup tracks and verification contracts.' }),
  operation({ id: 'team-access', name: 'Team Access', domain: 'administration', summary: 'Owner-controlled staff invitations, roles, scopes, and account security.', users: ['owner', 'administrator'], surfaces: { dashboardRoutes: ['/settings/team'], botFeatures: ['team-invitations'], scenarioProofs: ['administration-team-management'] }, journey: 'An owner invites staff, assigns a role-specific scope, and revokes access with immediate session enforcement.', invalidState: 'An invitation is expired, replayed, cross-guild, self-escalating, or grants authority above the actor.', discordBehavior: 'Staff Discord actions enforce the same guild and role scope as the dashboard.', dashboardBehavior: 'Members, invitations, role views, last activity, sessions, and revocation are editable and auditable.', syntheticEvidence: 'Fixtures prove atomic acceptance, replay rejection, scope changes, revocation, and cross-guild isolation.', liveEvidence: 'A test staff account accepts an invite, performs one allowed action, fails one denied action, and is revoked.', crossFeature: 'Authorization scopes constrain every dashboard, Discord, commerce, support, and audit operation.' }),
  operation({ id: 'automations', name: 'Automations', domain: 'automation', summary: 'Typed triggers, conditions, actions, recursion prevention, and execution history.', users: ['administrator'], surfaces: { dashboardRoutes: ['/automations'], botFeatures: ['automations'], scenarioProofs: ['administration-automations'] }, journey: 'An administrator composes, simulates, activates, and observes a typed trigger-condition-action flow.', invalidState: 'A graph recurses, conflicts, references missing resources, exceeds rate policy, or uses unauthorized actions.', discordBehavior: 'Triggered actions are deduplicated, bounded, attributed, and suppress recursive re-entry.', dashboardBehavior: 'Every trigger, condition, action, dependency, preview, run, error, and pause control is editable.', syntheticEvidence: 'Event graphs prove recursion, concurrency, cooldowns, missing references, provider failure, and compensation.', liveEvidence: 'A real Discord event executes one configured automation with operation-linked readback.', crossFeature: 'Moderation, economy, roles, scheduled content, webhooks, notifications, and commerce expose versioned automation contracts.' }),
  operation({ id: 'webhook-relays', name: 'Webhook Relays', domain: 'automation', summary: 'Authenticated external event ingestion and controlled Discord delivery.', users: ['owner', 'administrator'], surfaces: { dashboardRoutes: ['/webhook-relays'] }, journey: 'An owner creates a signed relay, sends a test event, and observes transformed Discord delivery.', invalidState: 'A signature, secret, payload, destination, transform, replay window, or rate limit is invalid.', discordBehavior: 'Accepted events deliver once to authorized destinations with safe content and mentions.', dashboardBehavior: 'Endpoints expose masked secret state, rotation, schema, transform, test, health, and replay evidence.', syntheticEvidence: 'Wire fixtures prove signatures, replay, malformed payloads, rate limits, rotation, and Discord failure.', liveEvidence: 'A signed external request produces one traceable Discord message and duplicate rejection.', crossFeature: 'Automations, incidents, notifications, moderation, and commerce may consume relays through typed events.' }),
  operation({ id: 'custom-commands', name: 'Custom Commands', domain: 'automation', summary: 'Guild-defined commands with typed inputs, outputs, permissions, and effects.', users: ['administrator', 'member'], surfaces: { dashboardRoutes: ['/commands'], botFeatures: ['custom-commands'], scenarioProofs: ['administration-custom-commands'] }, journey: 'An administrator defines and tests a command whose typed effect executes for an authorized member.', invalidState: 'A name collides, output exceeds Discord limits, an effect is unsafe, or permissions are ambiguous.', discordBehavior: 'Commands validate inputs, enforce scope, acknowledge promptly, and execute declared effects once.', dashboardBehavior: 'Names, options, permissions, responses, effects, cooldowns, conflicts, preview, and test are editable.', syntheticEvidence: 'Fixtures prove collisions, parsing, permissions, cooldowns, effects, limits, and degraded dependencies.', liveEvidence: 'A real custom command executes its configured response or effect with audit readback.', crossFeature: 'Automations, economy items, roles, moderation, embeds, and notifications expose safe callable effects.' }),
  operation({ id: 'operation-queue', name: 'Operation Queue and Workflows', domain: 'administration', summary: 'Traceable mutation lifecycle, retries, readback, rollback, and failed-action work.', users: ['owner', 'administrator', 'moderator', 'finance', 'support'], surfaces: { dashboardRoutes: ['/action-queue', '/workflows'] }, journey: 'Staff inspect an operation from request through runtime effect, retry, readback, and supported recovery.', invalidState: 'An operation lacks identity, actor, source, target, idempotency, dependency state, or recovery policy.', discordBehavior: 'Queued Discord work preserves acknowledgement and exposes delayed completion or failure.', dashboardBehavior: 'Draft, validation, conflict, preview, commit, execution, readback, audit, retry, and recovery are linked.', syntheticEvidence: 'Failure injection proves duplicate work, crash recovery, poison items, backoff, compensation, and fairness.', liveEvidence: 'A consequential live mutation exposes one operation identity across dashboard, database, Discord, and audit.', crossFeature: 'All significant mutations publish feature-specific stages and evidence into this shared lifecycle.', services: ['operation coordinator', 'durable action queue', 'readback reconciler'], tables: ['significant_operations', 'operation_events', 'configuration_releases'] }),
  operation({ id: 'audit-history', name: 'Audit and Administrative Changes', domain: 'administration', summary: 'Actor-attributed, operation-linked, guild-scoped, tamper-evident history.', users: ['owner', 'administrator', 'moderator', 'finance', 'support'], surfaces: { dashboardRoutes: ['/audit', '/admin-changes'], botFeatures: ['audit'], scenarioProofs: ['administration-audit'] }, journey: 'Authorized staff trace a consequential change from actor and intent to live effect and recovery.', invalidState: 'An event lacks actor, operation, guild, action, safe detail, ordering, or integrity linkage.', discordBehavior: 'Critical actions do not silently succeed when required audit persistence fails.', dashboardBehavior: 'Search, filters, evidence, integrity, retention, export, and related operations are available by scope.', syntheticEvidence: 'Fixtures prove append ordering, integrity chain, actor attribution, redaction, retention, and isolation.', liveEvidence: 'A real dashboard and Discord mutation appear as linked, attributable, integrity-verifiable events.', crossFeature: 'Every feature publishes its own audit event vocabulary while audit owns storage and integrity.' }),
  operation({ id: 'diagnostics-incidents', name: 'Diagnostics and Incidents', domain: 'administration', summary: 'Redacted system diagnostics, incident lifecycle, remediation, and recovery evidence.', users: ['owner', 'administrator', 'support'], surfaces: { dashboardRoutes: ['/diagnostics', '/incidents'], scenarioProofs: ['administration-diagnostics', 'administration-incidents'] }, journey: 'An operator detects degradation, opens an incident, applies guided remediation, and records recovery evidence.', invalidState: 'Diagnostics expose secrets or PII, an incident lacks affected scope, or recovery is claimed without readback.', discordBehavior: 'Affected commands provide safe degraded feedback and an operation or incident reference.', dashboardBehavior: 'Health, failures, severity, affected features, timeline, mitigation, recovery, and redacted export are visible.', syntheticEvidence: 'Injected failures prove alerting, deduplication, redaction, mode transitions, recovery, and closure gates.', liveEvidence: 'A reversible provider interruption produces and resolves an incident with real health readback.', crossFeature: 'System state, providers, queues, feature health, notifications, audit, backup, and recovery supply evidence.' }),
  operation({ id: 'system-settings', name: 'System Settings', domain: 'administration', summary: 'Editable owner configuration for providers, notifications, privacy, and global behavior.', users: ['owner', 'administrator'], surfaces: { dashboardRoutes: ['/settings'] }, journey: 'An owner edits a supported setting, validates dependencies, previews impact, saves, and observes activation.', invalidState: 'A value is malformed, unauthorized, secret-bearing in public state, or conflicts with a feature requirement.', discordBehavior: 'Runtime consumers reload activated settings without leaking credentials or requiring unrelated restarts.', dashboardBehavior: 'Every supported setting is editable with source, validation, masked secrets, impact, and save status.', syntheticEvidence: 'Schema fixtures prove all sections are editable, validated, scoped, redacted, and reloadable.', liveEvidence: 'A reversible setting change is activated and observed by its real runtime consumer.', crossFeature: 'Settings hold shared provider and policy state while each feature owns its own configuration schema.' }),
] as const;
