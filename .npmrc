# Keep pnpm's virtual-store package directory names bounded.  electron-builder
# emits absolute NSIS include paths from app-builder-lib; on Windows, the
# default peer-context directory can push those paths past MAX_PATH.
virtual-store-dir-max-length=60
